# 📦 rasberry_api_management

**rasberry_api_management** --- это сервис на базе Spring Boot,
созданный для автоматизации задач домашнего/пет-сервера (например,
Raspberry Pi).\
Основная задача проекта --- централизованное управление своим домашним/пет-сервером, например: Rclone-бэкапами
и отправка уведомлений о статусе выполнения задач в Telegram.

------------------------------------------------------------------------

## 🚀 Основные возможности

###  1. Обёртка над Rclone

Сервис предоставляет удобный интерфейс для работы с **Rclone**, включая:

-   запуск процессов синхронизации и бэкапа;
-   управление Rclone-профилями;
-   использование единого централизованного конфига.

------------------------------------------------------------------------

### ️ 2. Планировщик (Scheduler)

Встроенный cron-планировщик позволяет автоматизировать запуск бэкапов.

Поддержка:

-   настраиваемой CRON-строки (`rcloneCron`);
-   автозапуска бэкапа при старте (`startBackupAtStart`);
-   настройки технических параметров Rclone:
    -   `checkers`,
    -   `transfers`,
    -   `timeStats`.

------------------------------------------------------------------------

### 📢 3. Telegram-уведомления

Сервис отправляет оповещения в Telegram-канал:

-   📌 о начале выполнения бэкапа;
-   📌 об успешном завершении;

Настройки Telegram-бота полностью конфигурируемы через
`application.yml`.

------------------------------------------------------------------------

## ⚙️ Конфигурация (application.yml)

``` yaml
application:
  name: rasberry_api_management

telegram:
  bot:
    token: ${telegramToken}
    username: ${botUserName}

config:
  rclone:
    enable: ${rcloneEnable}
    startBackupAtStart: ${startBackupAtStart:false}
    profile: ${rcloneProfile}
    pathBackupFolder: ${pathBackupFolder}
    saveTokenInApp: ${saveTokenInApp}
    pathRcloneConfig: ${pathRcloneConfig}
    rcloneCron: ${rcloneCron}
    idChannelTelegram: ${idChannel}
    settings:
      timeStats: ${timeStats:5s}
      checkers: ${checkers:8}
      transfers: ${transfers:4}
```

------------------------------------------------------------------------

## 🛠 Используемый стек

-   **Java 17**\
-   **Spring Boot 3.5.3**\
-   **Spring Web / WebFlux**\
-   **Spring Actuator**\
-   **Lombok**\
-   **Apache Commons Configuration 2**\
-   **Rclone** (внешний инструмент)

------------------------------------------------------------------------

## 📈 Планы развития

-   Добавление REST API для управления задачами\
-   Панель мониторинга\
-   Управление сервисами Raspberry Pi\
-   Уведомления через другие мессенджеры\
-   Docker-образ